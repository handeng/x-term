# XTerm 功能规划

本规划以“串口调试可靠性优先”为原则：先完善所有串口场景都会使用的能力，再引入网络、传输协议和可视化等需要独立架构的模块。

## 本轮已实现

| 能力 | 实现方式 | 生产可用性要点 |
| --- | --- | --- |
| 完整串口参数 | 数据位扩展为 5–8；5 数据位配合第二档停止位时按 POSIX 语义使用 1.5 位，否则为 2 位 | 连接前校验非法数据位，参数修改后重连生效 |
| 多字符编码 | UTF-8、严格 7 位 ASCII、GB18030/GBK、ISO-8859-1 | 发送时禁止静默丢字；GB18030 接收支持跨读取块字符拼接 |
| 发送历史 | 每个会话保存最近 100 条，重复命令自动前置去重 | 恢复命令时同步恢复 ASCII/HEX、行尾和校验方式 |
| 文件发送 | TXT、CSV、二进制文件均按原始字节分块发送 | 后台串口写入、进度与取消、可配置块大小/间隔，不一次性载入大文件 |
| 接收触发回复 | 接收流帧尾或提示符支持 ASCII/HEX 匹配并自动回复 | 支持跨读取块匹配、校验码、行尾和冷却时间，防止回显触发风暴 |
| 实时日志 | 接收和发送同步写入 UTF-8 CSV | 独立批量写盘队列，4 MiB 待写上限，磁盘过慢时丢弃并写入告警而非无限占用内存 |

## 下一阶段建议

| 优先级 | 功能 | 规划与前置条件 |
| --- | --- | --- |
| P1 | 接收换行/不换行与查找定位 | 在 HEX 日志视图增加按帧、按字节数、按 CR/LF 分组，并提供查找、跳转和命中高亮；VT100 模式继续遵循远端控制序列，避免破坏终端语义 |
| P1 | Modbus 帧解析器 | 在现有 CRC 基础上增加 RTU 请求/响应模板、地址/功能码解析、CRC 状态标记和超时分帧；原始字节始终保留，解析层不可修改数据 |
| P1 | 日志回放 | 从现有 CSV/原始日志按原时间间隔或倍率回放，便于复现设备问题；需要定义稳定、带版本号的日志格式 |
| P2 | 数据波形 | 先提供可配置文本/二进制字段解析和采样率限制，再绘制多通道曲线；必须对点数和刷新率限流，避免高速数据导致内存或 CPU 异常 |
| P2 | TCP/UDP 调试 | 抽象 SerialPort 为统一 Transport，加入 TCP Client/Server、UDP 单播/广播；连接状态、日志和自动化复用现有实现 |
| P2 | 多串口并发 | 将当前单连接 AppModel 拆为每会话独立运行时，提供并发连接上限、总内存预算和聚合监控页 |
| P3 | XMODEM | 单独实现带状态机、超时、重试、取消和 CRC/Checksum 的传输模块，并通过伪终端做故障注入测试后再开放 |
| P3 | 固件升级模板 | 在 XMODEM 和 Modbus 稳定后增加 Bootloader 工作流；不同 MCU/厂商协议差异大，不将其硬编码进基础串口层 |

## 暂不直接照搬的项目

- Windows 的 `COM10+` 与管理员权限处理不适用于 macOS。XTerm 使用 `/dev/cu.*`，重点是设备占用、USB 驱动和 macOS 权限提示。
- “RS-232/RS-485 模式”本身不是 macOS 串口 API 的开关。电气层由 USB 转换器决定；软件侧继续提供 RS-485 常用校验、帧触发和后续 Modbus 解析。
- 多串口、网络连接和波形不能简单塞入当前单连接界面，否则会破坏已验证的断开清理与内存边界，应按上表先完成运行时与传输层拆分。

## 验收原则

- Apple Silicon 与 Intel 通用构建均通过，最低 macOS 13。
- 串口断开、窗口关闭和文件发送取消后，不再投递数据或保留无界任务。
- 所有接收、日志、波形和待写队列都有明确的条数或字节上限。
- 新增会话字段必须兼容旧版本 `sessions.json`，升级后不丢失用户配置。
- 协议功能必须具备正常、超时、断连、校验失败和大文件测试。
